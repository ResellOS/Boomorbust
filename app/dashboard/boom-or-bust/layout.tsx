import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Weekly Projection Cards',
  description:
    'Coming soon — weekly Boom or Bust projection cards for every roster player. Share to X in one tap.',
};

export default function BoomOrBustComingSoonLayout({ children }: { children: React.ReactNode }) {
  return children;
}
